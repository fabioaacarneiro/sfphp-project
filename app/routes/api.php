<?php

/**
 * API Routes — REST Endpoints
 *
 * All routes here return JSON responses (REST APIs).
 * These routes are used to build single-page applications (SPAs)
 * or mobile app backends.
 *
 * Example API routes (uncomment to use):
 *
 * use SfphpProject\src\Router;
 *
 * // Get all users
 * Router::get("/api/users", "UserController", "index");
 *
 * // Get single user
 * Router::get("/api/users/id:number", "UserController", "show");
 *
 * // Create user
 * Router::post("/api/users", "UserController", "store");
 *
 * // Update user
 * Router::put("/api/users/id:number", "UserController", "update");
 *
 * // Delete user
 * Router::delete("/api/users/id:number", "UserController", "destroy");
 *
 * @package SfphpProject
 * @subpackage app/routes
 */

use SfphpProject\src\Router;

// Define your REST API routes here
// Example: Router::get("/api/users", "UserController", "index");
