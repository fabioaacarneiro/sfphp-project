<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ $title | default(APP_NAME) }}</title>

    <!-- SFCSS Framework -->
    <link rel="stylesheet" href="/assets/css/sfcss.css">

    <!--
        No web fonts and no icon font are loaded from a CDN. A page that
        advertises zero dependencies should not need three third-party
        requests to render, and the system font stack in SFCSS costs nothing,
        works offline and leaks no visitor data to another origin.
    -->
</head>
<body>
