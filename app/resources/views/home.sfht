@extends('layouts.base')

@block('content')
<div class="container mx-auto p-4 pt-6 md:p-6 md:pt-12">
    <div class="flex flex-col items-center">
        <h1 class="text-6xl font-bold">{{ APP_NAME }}</h1>
        <h2 class="text-3xl">Simple Framework PHP</h2>
        <div class="bg-gray-200 rounded-2xl p-4 mt-4 md:p-6 md:mt-6">
            <img width="500" src="{{ asset('images/php.png') }}" alt="php">
        </div>
    </div>
    <div class="container mt-4 md:mt-6">
        <div class="text-center">
            <h3 class="text-2xl">Framework focado no aprendizado de PHP.</h3>
            <p class="mt-2">
                Um framework minimalista para quem quer usar php puro.
            </p>
        </div>
    </div>
</div>
<div class="author text-center text-sm mt-4"><span class="author-name">Fabio Carneiro {{ date('Y') }} | ver. {{ APP_VERSION }}</span></div>
@endblock
